# EMOTIV Cortex API (WebSocket JSON-RPC client — no official PyPI SDK, using websocket-client + requests)
websocket-client>=1.8.0
requests>=2.31.0

# Signal processing / EEG
numpy>=1.26.0
scipy>=1.12.0
mne>=1.6.0
scikit-learn>=1.4.0

# Generative pipeline (ControlNet / latent diffusion)
torch>=2.2.0
diffusers>=0.27.0
transformers>=4.38.0
accelerate>=0.27.0

# Loop server <-> frontend bridge
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
websockets>=12.0

# Config / utils
pyyaml>=6.0.1
python-dotenv>=1.0.1

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
