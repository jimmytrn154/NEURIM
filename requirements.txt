# EEG acquisition
websocket-client>=1.8.0   # EMOTIV Cortex API (WebSocket JSON-RPC, no official PyPI SDK)
requests>=2.31.0
pylsl>=1.16.2             # BrainFlow/LSL stream source

# Signal processing (FAA reward computation)
numpy>=1.26.0
scipy>=1.12.0

# Optimizer (projection + evolution strategy / GP-BO upgrade)
scikit-learn>=1.4.0

# Fake reward source (keyboard-driven, for the build-order-step-1 loop)
pynput>=1.7.6

# Generation - procedural fallback (always available)
pillow>=10.2.0
# OpenAI image backend (generator.backend = "openai")
openai>=1.0.0

# Orchestrator <-> service bridge
websockets>=12.0

# Config / utils
pyyaml>=6.0.1
python-dotenv>=1.0.1

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
