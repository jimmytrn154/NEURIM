# Core (already present in the NEURIM .venv)
torch>=2.0
scipy>=1.10
numpy>=1.24
scikit-learn>=1.3

# Live EPOC X streaming via the EMOTIV Cortex API
websocket-client>=1.5

# Paper-faithful EMD + Hilbert-Huang feature path (offline training, --feature hht)
EMD-signal>=1.4
